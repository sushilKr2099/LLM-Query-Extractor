# LLM-Query-Extractor
This Python app uses the llama-3.1-8b-instant model to extract company names, performance metrics (like GMV, revenue, and profit), and date ranges from user queries. The data is structured into a JSON format. It supports multiple companies, default dates, and robust error handling for query variations, aiding effective data analysis and reporting.
